---
import GeometricAvatar from "./GeometricAvatar.astro";

const stats = [
  { value: "HUST '25", label: "Computer Science" },
  { value: "Samsung", label: "Project Leader" },
  { value: "2", label: "Startups Founded" },
];
---

<section id="about" class="section bg-[var(--color-bg-secondary)]">
  <div class="container mx-auto">
    <div class="grid lg:grid-cols-2 gap-12 lg:gap-16 items-center">
      <!-- Left: Avatar and Stats -->
      <div class="flex flex-col items-center lg:items-start gap-8" data-animate="slide-right">
        <GeometricAvatar />
        
        <!-- Stats row -->
        <div class="flex flex-wrap justify-center lg:justify-start gap-6">
          {stats.map((stat, i) => (
            <div 
              class="text-center lg:text-left"
              data-animate="fade-up"
              data-delay={String((i + 2) * 100)}
            >
              <div class="text-xl md:text-2xl font-bold text-[var(--color-accent)]">
                {stat.value}
              </div>
              <div class="text-sm text-[var(--color-text-muted)]">
                {stat.label}
              </div>
            </div>
          ))}
        </div>
      </div>
      
      <!-- Right: Text content -->
      <div class="space-y-6">
        <div data-animate="fade-up">
          <span class="text-sm font-mono text-[var(--color-accent)] tracking-wider uppercase">About Me</span>
          <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold mt-2 text-[var(--color-text-primary)]">
            Engineer × Founder
          </h2>
        </div>
        
        <div class="space-y-4 text-[var(--color-text-secondary)] text-base md:text-lg leading-relaxed">
          <p data-animate="fade-up" data-delay="100">
            I'm a software engineer with <span class="text-[var(--color-text-primary)] font-medium">system-level depth</span> and <span class="text-[var(--color-text-primary)] font-medium">product-building ambition</span>. Currently in my final year at Hanoi University of Science and Technology—Vietnam's top technical university.
          </p>
          
          <p data-animate="fade-up" data-delay="200">
            By day, I lead software projects at <span class="text-[var(--color-accent)]">Samsung R&D Center Vietnam</span>, coordinating global releases across the full stack—from BIOS to applications. By night, I'm co-founding startups in edtech and enterprise software.
          </p>
          
          <p data-animate="fade-up" data-delay="300">
            My journey started with limited resources—building my first PC on a budget, learning to fix things independently. That resourcefulness evolved into engineering discipline and a belief that <span class="text-[var(--color-text-primary)] font-medium">technology should be an equalizer</span>.
          </p>
        </div>
        
        <!-- Tags/interests -->
        <div class="flex flex-wrap gap-2 pt-2" data-animate="fade-up" data-delay="400">
          {["Systems Thinking", "AI-Augmented Dev", "Linux", "Building Products"].map((tag) => (
            <span class="tag">{tag}</span>
          ))}
        </div>
      </div>
    </div>
  </div>
</section>

