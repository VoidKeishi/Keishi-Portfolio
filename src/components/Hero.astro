---
// Hero section - Bold introduction with aura effects
---

<section id="hero" class="relative min-h-screen flex items-center justify-center overflow-hidden">
  <!-- Background effects -->
  <div class="absolute inset-0 bg-hero-gradient opacity-50"></div>
  <div class="absolute inset-0 bg-grid opacity-30"></div>
  
  <!-- Animated aura orbs -->
  <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-[var(--color-aura-1)] rounded-full blur-[100px] animate-pulse-slow"></div>
  <div class="absolute bottom-1/4 right-1/4 w-80 h-80 bg-[var(--color-aura-2)] rounded-full blur-[80px] animate-pulse-slow animation-delay-1000"></div>
  <div class="absolute top-1/2 right-1/3 w-64 h-64 bg-[var(--color-aura-3)] rounded-full blur-[60px] animate-pulse-slow animation-delay-2000"></div>
  
  <!-- Content -->
  <div class="relative z-10 container mx-auto px-4 text-center">
    <!-- Eyebrow text -->
    <p 
      class="text-sm md:text-base font-mono text-[var(--color-accent)] mb-6 tracking-wider uppercase"
      data-animate="fade-up"
    >
      Software Engineer • Founder
    </p>
    
    <!-- Main heading -->
    <h1 
      class="text-4xl sm:text-5xl md:text-7xl lg:text-8xl font-bold mb-4 tracking-tight"
      data-animate="fade-up"
      data-delay="100"
    >
      <span class="text-[var(--color-text-primary)]">Phạm Phan Anh</span>
    </h1>
    
    <!-- Nickname with gradient -->
    <h2 
      class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl font-bold mb-8"
      data-animate="fade-up"
      data-delay="200"
    >
      <span class="text-gradient">Keishi</span>
    </h2>
    
    <!-- Tagline -->
    <p 
      class="text-lg md:text-xl lg:text-2xl text-[var(--color-text-secondary)] max-w-2xl mx-auto mb-12"
      data-animate="fade-up"
      data-delay="300"
    >
      Building technology that matters — with system-level depth and product-building ambition.
    </p>
    
    <!-- CTA Buttons -->
    <div 
      class="flex flex-col sm:flex-row items-center justify-center gap-4"
      data-animate="fade-up"
      data-delay="400"
    >
      <a href="#connect" class="btn btn-primary group">
        <span>Get in touch</span>
        <svg class="w-4 h-4 transition-transform group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
        </svg>
      </a>
      <a href="#about" class="btn btn-secondary">
        Learn more
      </a>
    </div>
    
    <!-- Decorative aura line -->
    <div class="mt-16 max-w-md mx-auto" data-animate="scale" data-delay="500">
      <div class="aura-line h-px aura-line-animated"></div>
    </div>
  </div>
  
  <!-- Scroll indicator -->
  <div class="absolute bottom-8 left-1/2 -translate-x-1/2" data-animate="fade" data-delay="600">
    <a href="#about" class="flex flex-col items-center gap-2 text-[var(--color-text-muted)] hover:text-[var(--color-accent)] transition-colors">
      <span class="text-xs font-mono uppercase tracking-widest">Scroll</span>
      <svg class="w-5 h-5 animate-bounce" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
      </svg>
    </a>
  </div>
</section>

<style>
  @keyframes pulse-slow {
    0%, 100% { 
      opacity: 0.5;
      transform: scale(1);
    }
    50% { 
      opacity: 0.8;
      transform: scale(1.1);
    }
  }
  
  .animate-pulse-slow {
    animation: pulse-slow 8s ease-in-out infinite;
  }
  
  .animation-delay-1000 {
    animation-delay: 1s;
  }
  
  .animation-delay-2000 {
    animation-delay: 2s;
  }
</style>

